# Scripts

Place repeatable database operation scripts here.

Use PowerShell 7 for project scripts.

- `bootstrap-dev.ps1` creates the local database container and applies the primary contacts table definition.
